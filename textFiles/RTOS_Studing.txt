Initialize a Mutex:
    SemaphoreHandle_t your_name_Mutex;


Create a Mutex
    
    xSemaphoreHandle your_name_Mutex = xSemaphoreCreateMutex();
    if (your_name_Mutex == NULL)
    {
        // Handle error creating mutex
        Serial.println("Failed to create mutex");
    }



Initialize a Queue
    QueueHandle_t your_name_Queue;

Create a Queue

    your_name_Queue = xQueueCreate(queue_size, sizeof(Struct_object_t)); 
    if (your_name_Queue == NULL)
    {
        // Handle error creating queue
        Serial.println("Failed to create queue");
    }
